# Trạng thái dự án — Hóa Học Cùng Em

_Cập nhật: 2026-07-12 (sau khi hoàn thành GD1-GD3, MR-A→MR-M, trừ MR-K, MR-N mining nội dung thật cho Lớp 11, MR-O mở rộng xuống Lớp 6-7, MR-P mining thêm câu hỏi Lớp 8-9, MR-Q khảo sát kho "HSG chuyên đề", MR-R nâng cấp sơ đồ tư duy + cấu trúc đề mới CT GDPT 2018 + hình ảnh/slide tài liệu, MR-S/MR-T hoàn thiện hạ tầng AI soạn câu hỏi + mining KHTN 8, MR-U sửa lỗi bảo mật/toàn vẹn + import lớp 12, MR-V bảng markdown + ảnh + rate-limit Postgres, MR-W — sửa thời lượng đề kiểm tra sai lệch, tách đăng nhập học sinh/giáo viên, tạo 3 skill chuyên biệt (lý thuyết/bài tập/hình ảnh), nâng cấp mẫu lý thuyết+bài tập cho Lớp 8 và Lớp 12, sửa 1 lỗi hydration thật, MR-X — sửa lỗi đăng nhập giáo viên bị 404, áp dụng khung mục lý thuyết đầy đủ hơn (TCVL/TCHH/trạng thái tự nhiên/điều chế) cho 4 bài Lớp 8 Chương II dựa trên SGK KNTT + file ôn tập THCS giáo viên tự soạn, MR-Y — hoàn tất nâng cấp toàn bộ Lớp 8 Chương I (6 bài) với Ứng dụng thực tế + Bài tập áp dụng nhanh, coi như Lớp 8 đã xong theo yêu cầu người dùng, MR-Z — hoàn tất nâng cấp toàn bộ 28 bài còn lại của Lớp 12 (8 chương), và MR nâng cấp giao diện sơ đồ tư duy (màu/icon) + MR-AA→MR-AD — hoàn tất nâng cấp lý thuyết (Bài tập áp dụng nhanh + ứng dụng thực tế) cho TẤT CẢ các lớp còn lại (9, 10, 6, 7, 11) theo đúng yêu cầu "tự động làm tiếp các lớp còn lại" — **cả 7 khối lớp (6-12), 96 bài, đã hoàn tất nâng cấp lý thuyết**). File này là "bức tranh toàn cảnh" — đọc file này trước để nắm thực trạng, thay vì đọc lại toàn bộ code._

## 1. Dự án là gì

Website học Hóa học lớp 6–12 (mở rộng từ phạm vi ban đầu 8–12 kể từ MR-O). Ban đầu là công cụ nhỏ cho <10 học sinh do giáo viên quản lý thủ công; từ 2026-07-10 đã mở rộng thêm đăng ký tự do có thu phí, bảo mật nâng cao, và đang tiếp tục mở rộng cộng đồng/phân luồng học tập theo `KE_HOACH_MO_RONG.md`. Xem file đó để biết kế hoạch đầy đủ và lý do các non-goal cũ trong `CLAUDE.md` được ghi đè.

## 2. Công nghệ đang dùng

- Next.js 16 (App Router) + TypeScript + Tailwind CSS
- **PostgreSQL (Supabase)** qua Prisma ORM (đã đổi từ SQLite để deploy công khai được — SQLite không phù hợp hosting serverless như Vercel). Schema tại `prisma/schema.prisma`, kết nối qua `DATABASE_URL` (pooled) + `DIRECT_URL` (trực tiếp, dùng cho migration)
- Auth.js (NextAuth v5) — Credentials provider, mật khẩu hash bcrypt, JWT session
- **Google Gemini** (`@google/generative-ai`) cho module AI hỏi đáp — đã đổi từ Anthropic. **Cần điền `GEMINI_API_KEY` thật vào `.env`** (lấy miễn phí tại https://aistudio.google.com/apikey) — hiện đang để trống, chưa test thật.
- **PWA**: manifest + service worker tối giản (chỉ cache icon/manifest, không cache trang có xác thực) — cài được vào máy/điện thoại qua "Cài đặt ứng dụng" của trình duyệt
- Code đã đẩy lên `https://github.com/tranphg-gub/web-hoa-hoc` (nhánh `main`), local branch `master` tracking `origin/main`
- Chạy local qua `npm run dev` (http://localhost:3000)

## 3. Tính năng đã hoạt động

| Module | Trạng thái | Ghi chú |
|---|---|---|
| Đăng nhập/phân quyền | ✅ | 2 vai trò ADMIN/STUDENT, `proxy.ts` chặn route theo vai trò |
| Bảo mật tài khoản (MR-A) | ✅ | Khóa đăng nhập 15 phút sau 5 lần sai/username; bắt buộc đổi mật khẩu lần đầu (`mustChangePassword`); trang `/change-password` |
| Đăng ký tự do + học phí (MR-D) | ✅ (khung, chưa gateway thật) | `/register` công khai, 99.000đ/lớp, tài khoản tự đăng ký bị khóa ở `/payment-pending` cho tới khi admin xác nhận thủ công tại `/admin/payments`. Tài khoản admin tạo vẫn miễn phí như cũ |
| Tài liệu học tập | ✅ | Lớp → Chương (model `Chapter` riêng) → Bài; tiến độ "đã học x/y bài" theo chương; nút tải Markdown + in PDF (MR-B); từ MR-R có thêm chế độ xem **Trình chiếu** (dạng slide, chia theo tiêu đề `##`) song song với **Bài viết**, và cú pháp chèn ảnh minh họa `![mô tả](url)` |
| Bài kiểm tra tính giờ | ✅ | Deadline tính từ server (`lib/quiz-scoring.ts`, có unit test), tự nộp khi hết giờ, đánh dấu nộp trễ. Từ MR-R hỗ trợ **3 dạng câu hỏi theo cấu trúc đề mới CT GDPT 2018**: trắc nghiệm 4 đáp án (Phần I), Đúng/Sai 4 ý chấm điểm không tuyến tính (Phần II — đúng 1/2/3/4 ý = 0,1/0,25/0,5/1 điểm câu đó), trả lời ngắn (Phần III) |
| Mức độ câu hỏi (Difficulty) | ✅ | 4 mức (Nhận biết/Thông hiểu/Vận dụng/Vận dụng cao), badge màu riêng, thống kê đúng/tổng theo mức độ ở trang kết quả |
| Trò chơi ghi nhớ | ✅ | 3 dạng: lật thẻ, ghép cặp, đố nhanh |
| AI hỏi đáp | ⚠️ Code xong (Gemini), **chưa có API key thật** | System prompt đã cải thiện: danh pháp KNTT, luôn cân bằng PTHH, thừa nhận khi không chắc |
| Admin — học sinh/thanh toán/chương/tài liệu/đề/flashcard | ✅ | CRUD đầy đủ, dropdown chọn Chapter (không gõ tay), trang riêng quản lý danh mục Chương |
| Mind map | ✅ | Theo `chapterId`, cả trang học sinh và admin. Từ MR-R có **3 kiểu hiển thị** chuyển đổi qua tab: Sơ đồ cây (org-chart), Sơ đồ vòng tròn (radial, SVG), Dòng thời gian (bản gốc) |
| Điểm thưởng & xếp hạng (MR-E) | ✅ | Cộng điểm khi nộp bài kiểm tra (score×10, ×5 nếu nộp trễ), trang `/leaderboard` nội bộ |
| Diễn đàn (MR-F) | ✅ | `/forum` — đăng câu hỏi, học sinh trả lời lẫn nhau, nút "Hỏi AI gia sư" trả lời ngay trong luồng bình luận |
| Nhắc nhở học tập (MR-G) | ✅ | Banner trên dashboard: cảnh báo lâu chưa học (`lib/reminders.ts`, có unit test) + liệt kê đề kiểm tra chưa làm + đề đánh giá định kỳ chưa làm |
| Test đầu vào/đánh giá định kỳ + AI phân tích lộ trình (MR-H) | ✅ | `Quiz.kind` (REGULAR/PLACEMENT/MONTHLY_CHECK); nộp bài PLACEMENT/MONTHLY_CHECK tự động gọi "AI con" (`lib/ai/placement-analysis.ts`) phân tích điểm yếu + đề xuất ôn tập, hiển thị ở trang kết quả |
| GitHub (MR-J) | ✅ | Đã push lên `https://github.com/tranphg-gub/web-hoa-hoc` nhánh `main` |
| PWA (MR-L) | ✅ | Cài đặt được vào máy/điện thoại, xem mục 2 |
| Nội dung chi tiết hơn (MR-I) | ✅ | ~40/83 bài được bổ sung ví dụ số liệu cụ thể/ứng dụng thực tế; đã đối chiếu web tham khảo (vietjack, loigiaihay,...) và kiểm tra lại toàn bộ phương trình mới cân bằng đúng |
| Ngân hàng câu hỏi + Bài tập luyện tập + AI tạo đề (MR-M) | ✅ | Mở rộng 27 đề kiểm tra (từ ~4-5 câu lên 7-9 câu/đề); thêm mục `/practice` cho học sinh (làm tự do theo chương, biết đúng/sai + giải thích ngay, không tính giờ/điểm) với ~2-3 câu/chương khởi điểm; trang admin `/admin/practice` quản lý ngân hàng + `/admin/practice/generate` dùng AI (Gemini, system prompt riêng) soạn nháp câu hỏi, giáo viên duyệt trước khi lưu |
| Mining đề thi thật cho Lớp 11 (MR-N) | ✅ | Trích xuất từ kho `Tài liệu/Lớp 11/...` (PDF ảnh scan, đọc bằng thị giác + tự render trang qua `pdftoppm` khi cần): 26 đề, 648 câu hỏi trắc nghiệm thật (đề thi thử của giáo viên TYHH, Phạm Văn Trọng, Dương Quốc Trọng, và đề thi thật Sở GD-ĐT Quảng Ninh 2023-2024), qua script `scripts/import-real-exams.mjs` (idempotent, chạy lại an toàn). Các câu tự luận và câu Đúng/Sai (cấu trúc đề mới) bị bỏ qua vì không tương thích schema hiện tại; các câu có độ chắc chắn thấp (đọc phổ/cấu trúc mơ hồ) cũng bị bỏ qua để ưu tiên chất lượng hơn số lượng |
| Mining thêm Lớp 8-9 từ tài liệu vừa upload (MR-P) | ✅ | Từ `Tài liệu/Lớp 6-9 (THCS)/Mới upload (Lớp 6,7,8,9)/`: (1) bộ "440 bài tập trắc nghiệm" (đọc `.doc` cũ bằng `antiword -m UTF-8.txt`) — thêm 44 câu Oxide/Acid/Base/Muối vào quiz Chương II Lớp 8 và 21 câu Kim loại vào quiz Chương 6 Lớp 9; (2) file "Đại trà" Bài 25 Phân bón hoá học — viết lại lý thuyết chi tiết hơn cho Document Bài 12 + tạo mới 1 quiz riêng 34 câu (trước đó Bài 12 chưa có quiz); (3) file "Đại trà" Bài 14 Biến đổi vật lí/hoá học — thêm 30 câu vào quiz Chương I Lớp 8 (chỉ lấy câu có đáp án A/B/C/D tường minh trong tài liệu gốc, bỏ qua câu bị lỗi định dạng danh sách khi trích xuất docx và 1 câu có phương án không khớp đáp án hoá học). Script: `scripts/add-lop8-9-questions.mjs`, `scripts/add-phanbon-quiz.mjs`, `scripts/add-chuong1-questions.mjs` (đều idempotent, tự bỏ qua nếu đã chạy) |
| Khảo sát kho "HSG chuyên đề" (MR-Q) | ✅ (khảo sát, phần lớn bị loại) | Trích xuất 9 chuyên đề gắn nhãn Lớp 8 (Mol, Nồng độ dung dịch, Độ tan, Tính theo PTHH, Năng lượng hoá học, Tốc độ pư/cân bằng, Bảo toàn nguyên tố, Oxide, Acid). Kết luận: phần lớn nội dung là tự luận nhiều bước, nhiều câu tái sử dụng đề thi THPT Quốc gia/đại học (vượt xa mức lớp 8), và lời giải bị mất số liệu khi trích xuất do dùng Equation Editor (OMML) mà `python-docx` không đọc được — không đủ tin cậy để tự giải và đưa vào quiz. Chỉ giữ lại 12 câu từ Chuyên đề 10 (Mol/tỉ khối) đúng mức lớp 8, 1 bước tính, tự kiểm chứng độc lập được. Xem chi tiết mục 3 bên dưới |
| Nâng cấp AI soạn đề (MR-S, 2026-07-11) | ✅ code xong **và đã test với API thật** | Người dùng phản hồi "AI soạn đề không ổn". Nguyên nhân gốc: generator cũ chỉ đưa TÊN CHƯƠNG vào prompt, không có nội dung bài học. Đã viết lại (`lib/ai/exercise-generator.ts`, `lib/ai/generation-context.ts`, `lib/ai/quiz-question-generator.ts`): (1) prompt kèm nội dung Document của chương (RAG đơn giản, ~18k ký tự) + danh sách câu đã có để chống trùng + định nghĩa 4 mức độ + quy tắc danh pháp KNTT; (2) structured output qua `responseSchema` (hết parse JSON bằng regex); (3) xáo vị trí đáp án phía server; (4) lượt AI thứ 2 giải "mù" từng câu để kiểm chứng đáp án — badge ✓/⚠ trên từng câu nháp; (5) form "AI soạn nháp" mới ngay trong `/admin/quizzes/[id]` hỗ trợ cả 3 dạng câu CT GDPT 2018 (chọn số câu Phần I/II/III), AI tự gán mức độ từng câu, giáo viên duyệt/sửa rồi mới lưu; (6) validate lại server-side trước khi lưu. **Test thật 2026-07-11**: `GEMINI_API_KEY` trong `.env` bị lỗi định dạng (thiếu dấu ngoặc kép đóng) — đã sửa; model `gemini-2.5-flash`/`gemini-2.5-pro` không còn dùng được cho API key mới ("no longer available to new users") — đổi sang alias `gemini-flash-latest` (tự trỏ tới bản flash mới nhất, tránh phải cập nhật tên model theo thời gian). Gọi thử qua trình duyệt thật (Chrome do Claude điều khiển): request đầu tiên thành công (200, ~60s cho 4 câu TN + 1 Đúng/Sai + 2 Trả lời ngắn kèm kiểm chứng); request thứ 2 ngay sau đó bị 429 — phát hiện quan trọng: **free tier Gemini giới hạn 20 REQUEST/NGÀY/model** (không phải /phút như dự đoán ban đầu) — xem mục 4. |
| Mining KHTN 8 - Tài liệu học tập 2024 (MR-T, 2026-07-11) | ✅ **đã chạy — 143 câu đã nạp vào database** | Theo hướng "ưu tiên đề thật": trích xuất từ `KHTN 8 - HÓA HỌC - TÀI LIỆU HỌC TẬP 2024.docx` (text sạch, có bảng đáp án). **143 câu trắc nghiệm có đáp án** cho ngân hàng `/practice` lớp 8 (Chương I: 63, Chương II: 80). Quy trình lọc: khôi phục chữ cái phương án từ XML numbering; đáp án lấy từ bảng key + đoạn "Đáp án: X"; riêng Bài 6 khôi phục từ lời giải bằng khớp số rồi TỰ GIẢI KIỂM CHỨNG từng câu (loại 3 câu lời giải lệch số với đề); loại toàn bộ câu có hình/OMML/cụt ngữ cảnh/trùng lặp/key sai chuẩn SGK. Script: `node scripts/add-khtn8-tlht-practice.mjs` (idempotent) — đã chạy, thêm đúng 143 câu, 0 trùng. Ngân hàng `/practice` Lớp 8 từ 10 → 153 câu. Còn khai thác thêm được KHTN 6 và KHTN 9 TLHT cùng bộ (chưa làm). |
| Sơ đồ tư duy đa dạng + cấu trúc đề mới + hình ảnh/slide tài liệu (MR-R) | ✅ | Phản hồi trực tiếp từ giáo viên khi dùng thử web thật: (1) sửa lỗi trang `/documents` mặc định cứng lớp 8 khi tài khoản không gắn lớp (admin/giáo viên) — nay có bộ chọn lớp; (2) sơ đồ tư duy thêm 2 kiểu hiển thị mới (cây, vòng tròn) cạnh dòng thời gian cũ; (3) schema `Question` thêm `type` (SINGLE_CHOICE/TRUE_FALSE_GROUP/SHORT_ANSWER) + trường `statements`/`shortAnswer`, áp dụng cho **mọi lớp** — giáo viên soạn được cả 3 dạng câu hỏi theo đúng cấu trúc đề thi THPT 2025 (CT GDPT 2018), đã test end-to-end (tạo đề trộn 3 dạng, làm bài, chấm điểm đúng 10/10); (4) `DocumentContent` hỗ trợ cú pháp ảnh `![mô tả](url)` + trang tài liệu có chế độ xem **Trình chiếu** (slide, điều hướng bàn phím/nút/chấm tròn); đã bổ sung 5 ảnh thật (Wikimedia Commons, CC0/public domain) vào 5 bài tiêu biểu lớp 6/7/8/11 làm mẫu — chưa phủ hết 96 bài |
| Import lớp 12 "đề dự đoán TN THPT 2026" định dạng mới + sửa lỗi bảo mật/toàn vẹn (MR-U, 2026-07-11) | ✅ | Kế thừa dữ liệu đã chuẩn bị sẵn (`scripts/data/*.json`) từ phiên trước: `node scripts/import-lop12-de-du-doan.mjs` thêm **38 đề / 494 câu Lớp 12** đúng cấu trúc CT GDPT 2018 (SINGLE_CHOICE + TRUE_FALSE_GROUP + SHORT_ANSWER thật, không phải câu test) — Lớp 12 từ 8→46 đề, 50→544 câu. Sau đó rà lại 1 báo cáo review độc lập (do người dùng dán), xác minh từng phát hiện bằng cách đọc code/query DB thay vì tin theo báo cáo, rồi sửa các lỗi xác nhận là thật: (1) **double-submit cộng điểm 2 lần** — `submitQuizAttempt` dùng `updateMany({ where: { id, submittedAt: null } })` để chỉ 1 trong N request đồng thời "thắng" (đã test trực tiếp bằng 2 request song song thật, xác nhận count đúng 1); (2) **học sinh xem được tài liệu/đề/flashcard của lớp khác** nếu biết ID — thêm `canAccessGrade()` (`lib/require-auth.ts`) và áp dụng cho 6 trang + `startQuizAttempt`; đã test bằng tài khoản thật (lớp 8 truy cập tài nguyên lớp 11 → 404, admin vẫn xem được mọi lớp); (3) **AI hỏi đáp lấy nhầm 20 tin nhắn ĐẦU TIÊN thay vì 20 tin GẦN NHẤT** (`orderBy: asc` thay vì `desc`) — khiến AI "quên" hội thoại sau tin thứ 20; (4) **xác nhận thanh toán không atomic** — gộp 2 update (Payment + User) vào `prisma.$transaction`; (5) sửa README.md (vẫn là mặc định create-next-app) và 1 dòng chữ mâu thuẫn ở trang chủ ("không công khai đăng ký" dù `/register` đã công khai thu phí từ MR-D). Các phát hiện khác trong báo cáo (rate limit chỉ in-memory, JSON string cho choices/answers, CI, dashboard giáo viên...) đã đánh giá nhưng cố tình chưa sửa lúc đó — rate-limit nay đã sửa ở MR-V bên dưới, phần còn lại xem mục 4 để biết lý do. |
| Bảng markdown trong tài liệu + thêm ảnh minh họa + rate-limit chuyển sang Postgres (MR-V, 2026-07-11) | ✅ | Người dùng xác nhận 56 câu trùng lặp giữa các đề dự đoán (từ MR-U) **không cần xóa** — trùng lặp 1 vài câu giữa các đề để ôn tập là chấp nhận được; không sửa gì thêm cho phát hiện này. Tiếp tục "nâng cấp": (1) **Bảng markdown** — `DocumentContent` trước đó không render được cú pháp `| ... |` khi bảng nằm chung khối `\n\n` với 1 dòng `##` phía trước (phát hiện khi thêm ảnh vào 1 bài Lớp 6 có bảng so sánh 3 thể của chất); đã viết lại vòng lặp xử lý dòng trong mỗi khối từ `.forEach` sang `while` thủ công để nhận diện bảng ở bất kỳ vị trí nào trong khối, thêm component `MarkdownTable`; test bằng ảnh chụp màn hình Chrome thật. (2) **Thêm 5 ảnh Wikimedia Commons** (đã `curl -I` xác nhận `image/svg+xml` trước khi dùng, script `scripts/add-more-images.mjs` idempotent) vào 5 bài Lớp 6/7/10/12 — biểu đồ % khí trong không khí, liên kết ion NaCl, mô hình phân tử nước 3D, hình dạng orbital s/p, sơ đồ pin điện hóa Zn-Cu; nâng tổng số bài có ảnh minh họa từ 5 → 10 (còn ~86/96 bài chưa có ảnh). (3) **Rate-limit chuyển từ bộ nhớ trong process sang Postgres** — thêm model `RateLimitHit` (migration `20260711103856_add_rate_limit_hit`), viết lại `lib/login-rate-limit.ts` và `lib/ai/rate-limit.ts` thành hàm async đọc/ghi DB (tự dọn dẹp record hết hạn theo từng key mỗi lần gọi, không cần cron riêng); cập nhật 3 call site (`lib/auth.ts`, `app/api/ai/chat/route.ts`, `app/(app)/forum/actions.ts`) thêm `await`. Đây là việc **bắt buộc phải làm trước khi deploy serverless** (Vercel) đã ghi ở mục 4 — nay đã xong, không còn là điều kiện tiên quyết treo trước MR-K nữa. Đã test thật qua HTTP (không phải chỉ đọc code): giả lập 6 lần đăng nhập sai liên tiếp bằng tài khoản không tồn tại → 5 lần đầu ghi nhận đúng vào bảng `RateLimitHit` (kiểm tra trực tiếp qua Prisma), lần thứ 6 bị khóa (redirect lỗi `Configuration` — đúng do `authorize()` throw); sau đó xóa sạch dữ liệu test và xác nhận tài khoản thật (`giaovien`) vẫn đăng nhập bình thường. |
| Sửa thời lượng đề sai lệch + tách đăng nhập + 3 skill chuyên biệt + nâng cấp mẫu lý thuyết/bài tập Lớp 8+12 (MR-W, 2026-07-12) | ✅ | Phản hồi trực tiếp: "kiểm tra j mà 50 phút được 12 câu". (1) **Sửa thời lượng đề kiểm tra** — audit toàn bộ 99 đề, phát hiện 2 lỗi ngược chiều: 38 đề "dự đoán TN THPT 2026" Lớp 12 cố định 50 phút bất kể chỉ trích xuất được 9-18 câu (thiếu phần Đúng/Sai + Trả lời ngắn so với cấu trúc gốc 28 câu); ngược lại 3 đề "Kiểm tra: Chương" Lớp 8 bị mining script nối thêm tới 51-53 câu nhưng vẫn giữ 15 phút gốc. Viết `scripts/fix-quiz-durations.mjs` tính lại theo công thức 1,5/3/2 phút cho SINGLE_CHOICE/TRUE_FALSE_GROUP/SHORT_ANSWER, làm tròn 5 phút, tối thiểu 10 phút — cập nhật 70/99 đề, idempotent. (2) **Tách đăng nhập học sinh/giáo viên** — thêm tab chọn vai trò ở `/login` (`app/login/login-form.tsx`), đổi nhãn/placeholder theo tab chọn, sau khi đăng nhập thành công điều hướng theo **vai trò thật của tài khoản** (không phải tab đã chọn) để không phá quyền — đã test qua Chrome CDP cả 3 trường hợp (học sinh, giáo viên, chọn nhầm tab). (3) **Tạo 3 skill chuyên biệt** tại `.claude/skills/`: `chem-theory` (soạn lý thuyết — nguồn tham khảo, cú pháp DocumentContent, cấu trúc heading cho mind-map), `chem-exercises` (soạn bài tập — nguồn SBT ưu tiên, đa dạng loại câu, luôn tự kiểm chứng đáp án, công thức thời lượng), `chem-images` (tìm ảnh Wikimedia — quy trình xác minh, và bài học mới: ảnh JPEG gốc dung lượng lớn có thể bị Wikimedia chặn "Too many requests" khi tải qua trình duyệt, phải dùng URL thumbnail `/thumb/.../960px-...`). (4) **Nâng cấp thử nghiệm 1 bài Lớp 8 + 1 bài Lớp 12** theo skill mới để làm mẫu trước khi mở rộng — xem mục 3b bên dưới. (5) **Phát hiện + sửa 1 lỗi hydration thật** ở `PracticeRunner` (`app/(app)/practice/[chapterId]/practice-runner.tsx`): xáo thứ tự câu bằng `Math.random()` ngay trong `useState()` khiến server và client tính ra thứ tự khác nhau — chỉ lộ ra khi 1 chương có ≥2 câu (trước đó Chương 1 Lớp 12 chỉ có 1 câu nên không thấy lỗi). Sửa bằng cách giữ thứ tự gốc lúc SSR, chỉ xáo trong `useEffect` (chạy sau khi hydrate xong). (6) **Phát hiện quan trọng cần người dùng xác nhận**: tìm thấy 12 ảnh sơ đồ tư duy phong cách infographic (do 1 công cụ AI tạo ảnh khác vẽ, ký tên "TÔI ♥ HÓA HỌC") người dùng đã lưu trực tiếp vào thư mục gốc dự án — đây chính là "sơ đồ tư duy Lớp 12" được nhắc tới. Các ảnh này là tác phẩm ảnh AI-generated tĩnh (Peptide, Amino Acid, Carbohydrate, Bài 13 Vật liệu Polymer...), khác hẳn cơ chế hiện tại của app (`mind-map-view.tsx` tự dựng cây/vòng tròn/timeline bằng CSS/SVG từ heading `##` của Document) — muốn đạt được đúng phong cách này cần tích hợp thêm 1 dịch vụ sinh ảnh AI (chi phí + rủi ro nhất quán mới), không phải chỉnh sửa CSS. Xem mục 4 để biết câu hỏi cần người dùng quyết định. |

| Sửa lỗi đăng nhập giáo viên 404 + khung mục lý thuyết đầy đủ hơn cho 4 bài Lớp 8 (MR-X, 2026-07-12) | ✅ | Người dùng báo "lỗi không vào được web" ngay sau MR-W — nguyên nhân: tab "Giáo viên" ở `/login` điều hướng về `/admin`, nhưng route này không có `page.tsx` riêng (chỉ có các trang con `/admin/students`, `/admin/quizzes`...) nên bị 404. Sửa `defaultCallback` của ADMIN thành `/admin/students`, test lại qua Chrome CDP xác nhận vào đúng trang. Sau đó người dùng phản hồi tiếp: nội dung lý thuyết "tạm tạm nhma vẫn thiếu nhiều thứ" — mỗi bài về 1 "dạng chất" phải luôn có đủ: giới thiệu, trạng thái tự nhiên, TCVL, TCHH, điều chế, ứng dụng. Tìm thấy nguồn tham khảo mới người dùng chỉ tới: `Tài liệu/Lớp 6-9 (THCS)/Hóa cấp 2 - Bài tập PTHH và tính toán/Ôn tập kiến thức cấp 2(2).docx` — file ôn tập THCS giáo viên tự soạn, text sạch, súc tích nhưng đủ Kim loại/Oxide/Acid/Base/Muối (kể cả phần hay bị bỏ sót như muối bị nhiệt phân, H2SO4 đặc/HNO3 tính oxi hóa mạnh). Cập nhật skill `chem-theory` thêm mục 3 "Khung mục CỐ ĐỊNH" (7 mục bắt buộc) và nguồn tham khảo mới. Áp dụng khung này cho 4 bài Lớp 8 Chương II: viết lại hoàn toàn **Bài 8. Acid** (1250 → 4195 ký tự, đọc thêm SGK KNTT trang 35-37 lấy khái niệm/TCVL/ứng dụng, kết hợp file ôn tập lấy acid mạnh/yếu + H2SO4 đặc/HNO3 + 1 ảnh Wikimedia); bổ sung (không viết đè) **Bài 9. Base** (+ trạng thái tự nhiên, tác dụng oxide acid/muối, điều chế), **Bài 10. Oxide** (+ trạng thái tự nhiên, oxide lưỡng tính, điều chế), **Bài 11. Muối** (+ trạng thái tự nhiên, bị nhiệt phân). Phát hiện và sửa 1 lỗi tự gây ra: dùng cú pháp `**in đậm**` kiểu markdown chuẩn trong nội dung — `DocumentContent` KHÔNG hỗ trợ cú pháp này (chỉ hỗ trợ `##`/`-`/PTHH/ảnh/bảng), khiến dấu `**` hiển thị literal trên trang; đã tự phát hiện qua ảnh chụp màn hình xác minh (không phải người dùng báo), sửa lại toàn bộ + ghi chú vào skill để không lặp lại. Đã test lại bằng ảnh chụp Chrome thật cho cả 4 bài, xác nhận render đúng. |

| Hoàn tất nâng cấp Lớp 8 Chương I — Ứng dụng thực tế + Bài tập áp dụng nhanh cho 6 bài (MR-Y, 2026-07-12) | ✅ | Người dùng gõ "ok bạn làm tiếp đi" — tiếp tục ngay sau MR-X. 6 bài Chương I (Phản ứng hóa học, Mol/tỉ khối chất khí, Dung dịch/nồng độ, Bảo toàn khối lượng/PTHH, Tính theo PTHH, Tốc độ phản ứng) là dạng "khái niệm/kỹ năng" chứ không phải "1 loại chất cụ thể" nên khung 7 mục của skill `chem-theory` không áp dụng y hệt — thay vào đó bổ sung theo mục 4 chung của skill: Ứng dụng thực tế + Bài tập áp dụng nhanh, và 1 vài mục nội dung còn thiếu so với SGK KNTT phát hiện khi dò trang 11-33: **Bài 2** thêm "Phản ứng tỏa nhiệt/thu nhiệt" + cảnh báo an toàn khí CO khi đốt nhiên liệu thiếu oxygen trong phòng kín; **Bài 3** thêm ví dụ Hình 3.1 (12g carbon/254g iodine/18g nước đều là 1 mol) + ứng dụng an toàn tỉ khối (CO2 nặng hơn không khí tích tụ hang sâu/giếng cạn, CH4 nhẹ hơn tích tụ trên cao — liên hệ trực tiếp tới các vụ ngạt khí thật); **Bài 4** thêm ví dụ tính nồng độ mol (trước đó thiếu) + ứng dụng nước muối sinh lý 0,9%; **Bài 5** thêm ví dụ vôi sống hút ẩm/CO2 minh họa định luật bảo toàn khối lượng trong đời sống; **Bài 6** thêm hẳn mục "Hiệu suất phản ứng" (có trong mục tiêu SGK nhưng bản cũ chưa viết) kèm công thức và ví dụ tính H%; **Bài 7** thêm 1 ảnh Wikimedia thật (bu lông sắt bị gỉ, CC-BY-SA 4.0, dùng URL thumbnail 960px để tránh lỗi "Too many requests" đã gặp ở MR-W/MR-X) minh họa so sánh tốc độ phản ứng nhanh (đốt cháy) và chậm (ăn mòn kim loại). Tất cả câu hỏi/ví dụ số liệu mới đều tự tính và kiểm tra lại (VD hiệu suất H% = 10,08/11,2 = 90%). Script `scripts/patch-lop8-chuong1.mjs` (idempotent, chèn theo marker, không viết đè nội dung cũ). Đã test lại bằng ảnh chụp Chrome thật xác nhận nội dung chèn đúng vị trí và ảnh hiển thị đúng. **Lớp 8 (11 bài) coi như đã hoàn tất nâng cấp theo yêu cầu người dùng** — bước tiếp theo tự nhiên là Lớp 12 (29 bài, mới làm mẫu 1 bài ở MR-W). |

| Hoàn tất nâng cấp toàn bộ Lớp 12 (28 bài còn lại, 8 chương) (MR-Z, 2026-07-12) | ✅ | Người dùng gõ "ok xong 12 thì sẽ tự động với các lớp còn lại" — tín hiệu tiếp tục xuyên suốt không cần hỏi lại giữa các chương. Áp dụng cùng quy trình đã kiểm chứng ở MR-W/X/Y cho toàn bộ 8 chương còn lại của Lớp 12: **Chương 1** (Bài 2. Xà phòng — viết lại đầy đủ cấu tạo/cơ chế micelle + 1 ảnh Wikimedia sơ đồ micelle, điều chế, nhược điểm/lưu ý, ứng dụng); **Chương 2** Carbohydrate (Bài 4,5,6 — bổ sung trạng thái tự nhiên/TCVL, ứng dụng thực tế, bài tập cho Glucose/Fructose/Saccharose/Maltose/Tinh bột/Cellulose); **Chương 3** Hợp chất chứa nitrogen (Bài 8,9,10 — Amine/Amino acid-Peptide/Protein-Enzyme, chú ý đặc biệt: **không sao chép chi tiết từ ảnh infographic tham khảo** vì phát hiện ảnh AI-generated có thể sai — VD 1 ảnh ghi nhầm "Alanine có 2 nhóm -NH2" trong khi thực tế chỉ có 1 nhóm, nên chỉ dùng ảnh tham khảo để biết cấu trúc mục, mọi nội dung hóa học tự kiểm chứng lại độc lập); **Chương 4** Polymer (Bài 12,13 — thêm PTHH trùng hợp/trùng ngưng cụ thể như tổng hợp nylon-6,6, cảnh báo ô nhiễm nhựa); **Chương 5** Pin điện/Điện phân (Bài 15,16 — thêm ứng dụng pin/ắc quy đời sống, điện phân sản xuất Al/NaOH/mạ điện); **Chương 6** Đại cương kim loại (Bài 18-22, 5 bài — đã khá đầy đủ từ trước, chỉ thêm Ứng dụng thực tế + Bài tập cho từng bài); **Chương 7** Nhóm IA-IIA (Bài 24,25 — thêm trạng thái tự nhiên, ứng dụng NaOH/Na2CO3/CaO/thạch cao, nước cứng); **Chương 8** Kim loại chuyển tiếp/phức chất (Bài 27,28,29 — thêm ứng dụng thực tế + bài tập, các bài này vốn đã tốt). 8 script `scripts/patch-lop12-chuong*.mjs`/`upgrade-lop12-bai2-*.mjs` (đều idempotent). Mọi số liệu bài tập mới đều tự tính kiểm tra lại trước khi đưa vào (không bịa số tròn). Đã test lại bằng ảnh chụp Chrome thật cho nhiều bài đại diện các chương, xác nhận render đúng. **Lớp 8 (11 bài) + Lớp 12 (29 bài) — toàn bộ 40 bài — coi như đã hoàn tất nâng cấp** theo đúng yêu cầu ban đầu của người dùng ("hãy bắt đầu với lớp 8 và lớp 12"). Người dùng đã xác nhận tiếp tục "tự động" sang các lớp còn lại (6,7,9,10,11) sau khi xong Lớp 12 — xem mục 4. |

| Nâng cấp giao diện sơ đồ tư duy + hoàn tất nâng cấp lý thuyết TẤT CẢ các lớp còn lại (2026-07-12) | ✅ | Người dùng nhắn "nếu hết token hãy đợi và làm tiếp nếu còn tiếp tục phát triển phần hình ảnh và sơ đồ tư duy", sau đó tiếp tục làm việc xuyên suốt không dừng. (1) **Nâng cấp giao diện sơ đồ tư duy tự sinh** (`components/chemistry/mind-map-view.tsx`) — mỗi bài trong 1 chương được tô 1 màu pastel riêng (xoay vòng 5 màu: hồng/tím/xanh dương/xanh lá/vàng) ở cả 3 kiểu hiển thị; mỗi mục con (heading) có icon đơn sắc chọn theo từ khóa (Khái niệm → sách, Trạng thái tự nhiên → lá cây, Tính chất → bình thí nghiệm, Điều chế → nhà máy, Ứng dụng → bóng đèn, Bài tập → bút). Phát hiện và sửa 1 lỗi UX thật khi test: Sơ đồ vòng tròn bị chồng chữ ở các bài có 7-10 mục con (do nội dung được viết chi tiết hơn qua các MR trước) — sửa bằng cách tăng kích thước canvas và giới hạn góc xoay mỗi bài đúng theo phần chia của nó thay vì 1 hằng số cố định. (2) **Hoàn tất nâng cấp lý thuyết cho toàn bộ 5 khối lớp còn lại** — mỗi khối chỉ cần thêm "Bài tập áp dụng nhanh" (trước đó HOÀN TOÀN chưa có ở bất kỳ bài nào của 5 khối lớp này) vì nội dung cốt lõi đã seed đúng/đủ từ các MR trước: **Lớp 9** (MR-AA, 15 bài/4 chương — Kim loại, Hydrocarbon, Ethylic alcohol/Acetic acid, Lipid-Carbohydrate-Protein-Polymer); **Lớp 10** (MR-AB, 16 bài/7 chương — mở rộng riêng Bài 7 "Xu hướng biến đổi thành phần và tính chất của hợp chất" từ 313 lên 974 ký tự, bổ sung phần hydride theo chu kì còn thiếu); **Lớp 6** (MR-AC, 7 bài/3 chương) và **Lớp 7** (MR-AC, 6 bài/4 chương — nội dung đã khá đầy đủ từ MR-O); **Lớp 11** (MR-AD, 19 bài/6 chương — Cân bằng hóa học, Nitrogen-Sulfur, Đại cương hữu cơ, Hydrocarbon, Dẫn xuất halogen-Alcohol-Phenol, Carbonyl-Carboxylic acid). Mỗi khối lớp dùng 1 script `scripts/patch-lop{N}-full.mjs` riêng (idempotent). Đã test lại bằng ảnh chụp Chrome thật cho các bài đại diện mỗi khối. **Toàn bộ 7 khối lớp (6,7,8,9,10,11,12), 96 bài học, đã hoàn tất nâng cấp lý thuyết** theo đúng lộ trình người dùng yêu cầu (bắt đầu Lớp 8+12 → tự động tiếp các lớp còn lại). |

## 4. Việc đang làm dở / kế hoạch tiếp theo

Xem `KE_HOACH_MO_RONG.md` mục 2 để biết đầy đủ. Chỉ còn:

- MR-K: Public deploy — **tạm dừng giữa chừng theo yêu cầu người dùng để rà soát lại nội dung trước**, cần tiếp tục: import project trên Vercel, set 5 biến môi trường (DATABASE_URL, DIRECT_URL, AUTH_SECRET, GEMINI_API_KEY, GEMINI_MODEL), bấm Deploy. Điều kiện tiên quyết "chuyển rate-limit sang Postgres" đã xong ở MR-V — không còn gì chặn kỹ thuật trước khi deploy nữa, chỉ còn chờ người dùng quyết định thời điểm.
- **Giới hạn quan trọng phát hiện 2026-07-11: Gemini free tier chỉ 20 REQUEST/NGÀY cho mỗi model** (không phải theo phút). `npm run db:assign-difficulty-ai` mới phân loại được ~240/1494 câu SINGLE_CHOICE trước khi hết quota trong ngày — cần chạy tiếp `npm run db:backup && npm run db:assign-difficulty-ai` vào (các) ngày sau để phân loại hết (đã tăng `BATCH_SIZE` lên 80 để đỡ tốn lượt gọi hơn). Muốn dùng AI nhiều hơn trong ngày (soạn đề hàng loạt, chat AI nhiều) thì cần nâng cấp gói trả phí Gemini API — tùy người dùng quyết định, Claude không tự ý bật billing.
- **Nguồn đề tốt chưa khai thác**: `KHTN 6 - TÀI LIỆU HỌC TẬP 2024.docx`, `KHTN 6 - CHUYÊN ĐỀ HỌC TẬP.docx`, `KHTN 9 - TÀI LIỆU HỌC TẬP 2024.docx` (cùng cấu trúc với KHTN 8 đã mining ở MR-T, parser tái sử dụng được); `trac nghiem-Hóa-dang moi.rar` chưa mở được (sandbox không có unrar).
- Bổ sung ảnh minh họa cho phần lớn trong số 96 bài học còn lại (đã làm 10/96 — 5 bài ở MR-R + 5 bài ở MR-V).
- **TẤT CẢ 7 khối lớp (6-12), 96 bài học, đã hoàn tất nâng cấp lý thuyết** (MR-X → MR-AD) — mọi bài đều có Bài tập áp dụng nhanh; các bài về "1 loại chất" (Acid/Base/Oxide/Muối...) đủ khung 7 mục (Giới thiệu/Trạng thái tự nhiên/TCVL/TCHH/Điều chế/Ứng dụng/Bài tập); các bài khái niệm/kỹ năng có Ứng dụng thực tế phù hợp. Đây là công việc "nâng cấp lý thuyết" lớn nhất trong lịch sử dự án — coi như đã đóng lại theo đúng yêu cầu ban đầu.
- **Nâng cấp giao diện sơ đồ tư duy tự sinh** (icon/màu sắc) — ✅ đã xong: màu pastel theo bài + icon theo mục, đã sửa lỗi chồng chữ ở Sơ đồ vòng tròn khi nhiều mục con.
- **Mining tiếp SBT Lớp 12** — đã giải nén và khảo sát `Tài liệu/Lớp 12/Hóa 12 theo chuyên đề/*.zip` (SBT thật 3 bộ KNTT/CTST/CD theo từng chương + nhiều tờ bài tập/đề luyện tập có đáp án), mới khai thác 10 câu từ 1 file (Chương 1) — còn 7 chương SBT chưa mining thành câu hỏi `/practice` (mới dùng để tham khảo viết lý thuyết). Lớp 8 không có SBT chính thức trong kho — dùng tiếp tài liệu tự soạn (KHTN 8 TLHT đã mining ở MR-T, còn KHTN 6/9 TLHT chưa mining).
- **Các phát hiện từ báo cáo review đã đánh giá nhưng cố tình CHƯA sửa** (không phải bỏ sót): `Payment.status` và các trường `choices`/`statements`/`answers` đang lưu String/JSON thay vì enum/bảng riêng — sửa được nhưng là refactor lớn, rủi ro cao hơn lợi ích trước mắt với quy mô <10 người dùng hiện tại; 26 nhóm câu hỏi trùng nội dung+đáp án giữa các đề "dự đoán TN THPT 2026" khác nhau (56/1504 câu, 3,7%) — **người dùng đã xác nhận (2026-07-11) không cần xóa**, coi là trùng lặp tự nhiên chấp nhận được giữa các đề ôn tập; CI (lint/test/build tự động), dashboard giáo viên, spaced repetition, mobile bottom-nav — đều là tính năng lớn cần người dùng xác nhận độ ưu tiên trước khi làm.

## 5. Việc CẦN người dùng làm

- Nếu muốn phân loại độ khó xong sớm hơn hoặc dùng AI soạn đề nhiều: cân nhắc nâng cấp gói Gemini API trả phí (free tier chỉ 20 request/ngày/model — xem mục 4). Không thì cứ để tự nhiên, chạy `npm run db:assign-difficulty-ai` mỗi ngày sẽ tự hoàn thành dần.
- Có mặt khi triển khai public (theo đúng yêu cầu của bạn) — chọn nền tảng hosting (Vercel là lựa chọn tự nhiên nhất cho Next.js). Rate-limit đã chuyển sang Postgres (MR-V) nên không còn điều kiện kỹ thuật nào phải chờ trước khi bấm Deploy.
- **Xác nhận hướng đi cho sơ đồ tư duy (MR-W)**: 12 ảnh mẫu tìm thấy trong thư mục gốc dự án là ảnh AI-generated tĩnh, đẹp và chi tiết hơn nhiều so với sơ đồ tự sinh hiện tại của app. Để đạt đúng phong cách đó, cần tích hợp 1 dịch vụ sinh ảnh AI (tốn phí theo lượt, và nội dung hóa học trong ảnh không tự sửa được như text nếu sai) — khác hẳn cách app đang làm (tự dựng từ heading, sửa văn bản là cập nhật được ngay). Cần người dùng chọn: (a) giữ cách tự sinh từ text như hiện tại, chỉ nâng cấp thêm icon/màu sắc cho đẹp hơn (rẻ, dễ bảo trì, không cần API mới); (b) tích hợp AI sinh ảnh để làm ra sơ đồ tĩnh đẹp như mẫu cho từng bài (cần API key riêng + chấp nhận chi phí + quy trình duyệt lại nội dung hóa học trong ảnh trước khi dùng); hoặc (c) kết hợp cả hai tùy trường hợp.
- **12 ảnh mẫu sơ đồ tư duy đang nằm trực tiếp ở thư mục gốc dự án** (tên file kiểu `1783785274882_..._....jpg`) — chưa bị xóa/di chuyển, cũng chưa đưa vào git. Nếu muốn giữ lại làm tài liệu tham khảo lâu dài, nên gom vào `Tài liệu/` (đã gitignore) cho gọn; nếu không cần nữa có thể tự xóa.
- Cung cấp thông tin chuyển khoản thật (số tài khoản ngân hàng...) nếu muốn hiển thị trên trang `/payment-pending` — hiện trang này chỉ ghi "liên hệ giáo viên", chưa có số tài khoản cụ thể vì tôi không tự bịa thông tin thanh toán.
- Nếu muốn dùng test đầu vào (MR-H) thật: tạo 1 Chapter "Ôn tập đầu năm" cho từng lớp, rồi tạo Quiz với "Loại đề" = Test đầu vào/Đánh giá định kỳ gắn vào chương đó.

## 6. Nội dung học tập hiện có trong database

| Lớp | Số bài | Đề kiểm tra | Câu hỏi (đề KT) | Bộ flashcard | Số thẻ | Câu luyện tập (`/practice`) |
|---|---|---|---|---|---|---|
| 6 | 7 | 3 | 36 | 3 | 30 | 0 |
| 7 | 6 | 4 | 47 | 4 | 40 | 0 |
| 8 | 11 | 3 | 138 | 2 | 15 | 153 |
| 9 | 15 | 4 | 50 | 4 | 17 | 13 |
| 10 | 16 | 7 | 41 | 7 | 18 | 19 |
| 11 | 19 | 32 | 648 | 6 | 18 | 10 |
| 12 | 22 | 46 | 544 | 8 | 20 | 19 |

Tổng: **1.504 câu hỏi đề kiểm tra** (1.494 SINGLE_CHOICE, 4 TRUE_FALSE_GROUP, 6 SHORT_ANSWER — hai dạng mới vẫn chiếm tỉ lệ rất nhỏ, chủ yếu từ vài đề "dự đoán TN THPT 2026" của Lớp 12) + **214 câu `/practice`** (thêm 10 câu SBT Lớp 12 ở MR-W).

Mỗi lớp có model `Chapter` riêng (grade, title, order) — Document/Quiz/FlashcardSet/PracticeQuestion liên kết qua `chapterId`. Câu hỏi đã có mức độ khó (gán theo vị trí câu trong đề: câu 1-2 Nhận biết, câu 3 Thông hiểu, câu 4+ Vận dụng/Vận dụng cao — giáo viên nên rà lại qua trang quản trị nếu muốn chính xác hơn theo từng câu cụ thể). Ngân hàng `/practice` mới có khởi điểm ~2-3 câu/chương — giáo viên có thể tự mở rộng thêm qua `/admin/practice/generate` (AI soạn nháp, duyệt trước khi lưu) mà không cần sửa code.

Riêng Lớp 11: 26 trong 32 đề (648 câu) là đề thi thật/thi thử sưu tầm từ giáo viên, gộp vào 1 chương dùng chung "Ôn tập tổng hợp Học kì 1" (không map được vào 1 chương cụ thể vì mỗi đề thường trải nhiều chương). Đáp án các đề không có sẵn đáp án án gốc (trừ bộ TYHH có khoanh tay) nên được giải bằng kiến thức hóa học — nên rà soát lại nếu phát hiện đáp án sai.

**Lớp 6-7 (mới, MR-O 2026-07-10)**: nội dung soạn từ tài liệu giáo viên cộng tác biên soạn (dự án KHTN 6789, phần Hóa học, người dùng upload) — mỗi bài có 1 Document (lý thuyết gốc do Claude viết lại dựa trên tài liệu tham khảo, không copy nguyên văn), gộp theo chương thành 1 Quiz + 1 FlashcardSet mỗi chương (đúng quy ước seed-thcs.ts cho lớp 8-9). Lớp 6: Chương 1 Chất quanh ta (bài 1-3), Chương 2 Vật liệu-nhiên liệu-nguyên liệu-lương thực thực phẩm (bài 4-5), Chương 3 Hỗn hợp (bài 6-7). Lớp 7: Chương 1 Nguyên tử-nguyên tố hóa học (bài 8-9), Chương 2 Bảng tuần hoàn (bài 10), Chương 3 Phân tử-liên kết hóa học (bài 11-12), Chương 4 Hóa trị-CTHH (bài 13). Chưa có `/practice`. Script: `npm run db:seed:lop6-7` (`prisma/seed-thcs-6-7.ts`).

**Lớp 8-9 mở rộng thêm (MR-P 2026-07-11)**: xem mục 3 hàng "Mining thêm Lớp 8-9". Bài 12 Phân bón hoá học từ chỗ chưa có quiz riêng nay có 34 câu; Chương I (Phản ứng hoá học, trọng tâm biến đổi vật lí/hoá học) và Chương II (Oxide/Acid/Base/Muối) đều được bổ sung nhiều câu hỏi thực tế có đáp án đối chiếu được. Do các câu mới được nối tiếp vào cuối đề (order 10+), script gán độ khó theo vị trí câu (mục 6) sẽ tự động gán phần lớn các câu này là "Vận dụng cao" dù độ khó thật có thể thấp hơn — hạn chế đã biết, chấp nhận được vì đây là cách toàn bộ ứng dụng đang xử lý các đề dài.

**Khảo sát kho "HSG chuyên đề" (MR-Q 2026-07-11)**: kho `Tài liệu/.../HSG/` là dự án nhiều giáo viên cộng tác biên soạn 70 chuyên đề luyện HSG/chuyên cho lớp 7-9, mới có 14 chuyên đề được nộp. Đã trích xuất và khảo sát kỹ 9 chuyên đề gắn nhãn phù hợp Lớp 8 (Mol/tỉ khối, Mối quan hệ m-n-V, Nồng độ dung dịch/pha chế/chuẩn độ, Độ tan/muối ngậm nước, Tính toán theo PTHH, Năng lượng hoá học, Tốc độ phản ứng/cân bằng hoá học, Định luật bảo toàn nguyên tố, Oxide, Acid — 2 bản). Phát hiện: (1) phần lớn bài tập là tự luận nhiều bước phức tạp (hỗn hợp nhiều chất, oxi hoá khử, hữu cơ), nhiều câu ghi rõ trích từ "Đề THPT QG"/"Đề TSĐH" (đề thi tốt nghiệp/tuyển sinh đại học, mức lớp 12) chứ không phải mức lớp 8 dù được xếp trong bộ tài liệu lớp 8; (2) lời giải trong file thường dùng Word Equation Editor (OMML) để viết công thức/số liệu — `python-docx` không đọc được nội dung này nên rất nhiều bước giải bị "mất số" khi trích xuất text, không đủ căn cứ để tự kiểm chứng đáp án mà không tự giải lại toàn bộ (rủi ro sai sót cao, không đúng tinh thần "chất lượng hơn số lượng"). Kết luận: **không đưa phần lớn nội dung HSG này vào quiz/practice** của app — chỉ giữ lại 12 câu trắc nghiệm 1 bước tính, đúng mức lớp 8, có thể tự kiểm chứng độc lập từ Chuyên đề 10 (Mol và tỉ khối chất khí), thêm vào quiz Chương I Lớp 8. Script: `scripts/add-hsg-lop8-questions.mjs`. Nếu sau này muốn khai thác tiếp kho HSG này, nên coi là nội dung "nâng cao/thi chuyên" tách riêng khỏi quiz đại trà, và người soạn cần tự giải lại từng bài (không dựa vào lời giải trích xuất) do hạn chế OMML nêu trên.

**Sơ đồ tư duy + cấu trúc đề mới + hình ảnh/slide (MR-R 2026-07-11)**: Sau khi người dùng tự mở web thật bằng tài khoản giáo viên và dùng thử, phát hiện 4 việc cần sửa/nâng cấp:
1. **Bug trang `/documents`**: `app/(app)/documents/page.tsx` mặc định `user.grade ?? 8` — tài khoản giáo viên/admin không gắn lớp cố định (`grade = null`) nên luôn bị kẹt ở lớp 8. Đã sửa: thêm bộ chọn lớp (`?grade=` query param) khi tài khoản không có `grade`, mặc định lớp 6.
2. **Sơ đồ tư duy quá đơn điệu**: bản cũ chỉ có 1 kiểu (dòng thời gian dọc). Đã thêm `components/chemistry/mind-map-view.tsx` với 3 kiểu chuyển qua tab: Sơ đồ cây (org-chart, CSS thuần), Sơ đồ vòng tròn (radial, dựng bằng SVG + `foreignObject`, có tính toán góc/bán kính theo số lượng bài và tiêu đề con), Dòng thời gian (giữ nguyên bản cũ).
3. **Cấu trúc đề mới CT GDPT 2018** (yêu cầu: áp dụng mọi lớp): thêm enum `QuestionType` (SINGLE_CHOICE/TRUE_FALSE_GROUP/SHORT_ANSWER) và các trường `statements`/`shortAnswer` vào model `Question` (migration `20260710203940_add_question_types`). Cập nhật toàn bộ luồng: form soạn câu hỏi admin (`add-question-form.tsx`, chuyển sang client component để đổi UI theo dạng), giao diện làm bài (`quiz-runner.tsx`), chấm điểm (`lib/quiz-scoring.ts` — hàm `scoreQuestion` mới, Đúng/Sai chấm theo thang không tuyến tính 0/0,1/0,25/0,5/1 giống cách Bộ GD&ĐT chấm đề thật), trang kết quả. Đã test end-to-end bằng script CDP điều khiển Chrome thật (đăng nhập học sinh, làm đề trộn 3 dạng, nộp bài) — điểm chấm đúng 10/10. Trong lúc test phát hiện và sửa 2 bug thật: (a) đóng gói câu trả lời Đúng/Sai bằng closure cũ gây mất thao tác khi bấm nhanh — sửa bằng `useRef` đồng bộ; (b) gọi server action bên trong callback của `setState` vi phạm quy tắc render của React (lỗi "Cannot update Router while rendering QuizRunner") — sửa bằng cách tách side-effect ra khỏi updater.
4. **Hình ảnh + chế độ trình chiếu cho tài liệu**: `DocumentContent` nhận thêm cú pháp `![mô tả](url)` (component `DocumentImage`); trang chi tiết bài học có `DocumentViewSwitcher` cho phép chuyển giữa "Bài viết" (như cũ) và "Trình chiếu" (`SlideViewer`, chia slide theo tiêu đề `##`, điều hướng bằng nút/phím mũi tên/chấm tròn). Đã bổ sung 5 ảnh thật từ Wikimedia Commons (giấy phép CC0/public domain, đã kiểm tra qua `curl -I` xác nhận `content-type: image/svg+xml` trước khi dùng) vào 5 bài mẫu: trạng thái chất (Lớp 6), nguyên tử + bảng tuần hoàn (Lớp 7), thang pH (Lớp 8), benzene (Lớp 11). Còn ~91/96 bài chưa có ảnh — người dùng cần cho biết có muốn tiếp tục bổ sung hàng loạt hay để giáo viên tự thêm dần qua trang admin.

## 7. Nguồn dữ liệu / seed script

```
npm run db:seed         # chỉ seed tài khoản (giaovien + 3 học sinh mẫu)
npm run db:seed:lop6-7  # Lớp 6-7 — tự chạy kèm gán độ khó
npm run db:seed:thcs    # Lớp 8-9  — tự chạy kèm gán độ khó
npm run db:seed:lop10   # Lớp 10
npm run db:seed:thpt    # Lớp 11-12
npm run db:assign-difficulty  # gán lại độ khó thủ công nếu cần (đã tự chạy sau mỗi seed ở trên)
node scripts/import-real-exams.mjs  # thêm đề thi thật lớp 11 (idempotent, không xóa dữ liệu cũ)
node scripts/add-khtn8-tlht-practice.mjs  # MR-T: 143 câu practice lớp 8 từ KHTN 8 TLHT 2024 (idempotent)
node scripts/import-lop12-de-du-doan.mjs  # MR-U: 38 đề / 494 câu "đề dự đoán TN THPT 2026" lớp 12 (idempotent)
node scripts/add-more-images.mjs          # MR-V: thêm ảnh minh họa Wikimedia vào tài liệu theo id (idempotent)
node scripts/fix-quiz-durations.mjs             # MR-W: tính lại durationSec theo số câu thực tế (idempotent, nên chạy lại mỗi khi thêm/bớt câu hỏi 1 đề)
node scripts/upgrade-lop8-bai11-muoi.mjs        # MR-W: nội dung lý thuyết mẫu Lớp 8 (idempotent)
node scripts/upgrade-lop12-bai1-ester.mjs       # MR-W: nội dung lý thuyết mẫu Lớp 12 (idempotent)
node scripts/add-lop12-sbt-ester-practice.mjs   # MR-W: 10 câu /practice từ SBT thật Lớp 12 (idempotent)
```

Chạy lại các lệnh seed theo khối lớp sẽ xóa và ghi đè Document/Quiz/Flashcard/Chapter của đúng khối đó.

**3 skill chuyên biệt** (MR-W, `.claude/skills/`) đóng vai trò quy trình chuẩn khi tiếp tục nâng cấp nội dung — không phải script chạy được, mà là hướng dẫn cho Claude khi được giao việc tương ứng:
- `chem-theory` — soạn/nâng cấp nội dung lý thuyết (Document): thứ tự nguồn tham khảo, cú pháp `DocumentContent`, cấu trúc heading để sơ đồ tư duy tự sinh đẹp.
- `chem-exercises` — mining/soạn bài tập (PracticeQuestion/Question): ưu tiên SBT thật, đa dạng loại câu, luôn tự kiểm chứng đáp án, công thức tính lại thời lượng đề.
- `chem-images` — tìm và chèn ảnh Wikimedia Commons: quy trình xác minh URL/giấy phép bắt buộc, cách tránh lỗi "Too many requests" với ảnh JPEG gốc lớn.

## 8. Tài khoản hiện có (dùng để test)

- Giáo viên: `giaovien` / `admin123`
- Học sinh mẫu: `hs_an` (lớp 8), `hs_binh` (lớp 10), `hs_chi` (lớp 12) — mật khẩu `hocsinh123`
- Học sinh do người dùng tự tạo thử qua Admin: `Phong 123` (lớp 9)

## 9. Thư mục tài liệu gốc

`Tài liệu/` (~4GB+, gitignore, không push lên git) — kho tham khảo thô theo Lớp → Chủ đề, dùng để đối chiếu khi soạn nội dung, chưa số hóa hết vào web. Thư mục `Tài liệu/Lớp 6-9 (THCS)/Mới upload (Lớp 6,7,8,9)/` chứa tài liệu giáo viên cộng tác biên soạn (dự án KHTN 6789) người dùng upload 2026-07-10 — đã khai thác phần "Đại trà" (cả 2 file Lớp 8: Bài 25 Phân bón, Bài 14 Biến đổi vật lí/hoá học), bộ "440 bài tập trắc nghiệm" Lớp 8-9 (MR-O, MR-P), và đã khảo sát kỹ thư mục "HSG" (MR-Q, kết luận phần lớn không phù hợp — xem mục 3). Còn chưa khai thác: các file KHTN 6/8/9 "TÀI LIỆU HỌC TẬP/DẠY THÊM" cỡ lớn (28-73MB/file, chưa mở — có thể chứa nội dung khác với bộ HSG/Đại trà nên vẫn đáng xem xét nếu cần thêm nội dung).

## 10. Cách chạy thử

```bash
npm install
npm run dev           # http://localhost:3000
npx tsc --noEmit      # kiểm tra kiểu
npm run build         # build production
npm run test          # unit test (quiz-scoring)
```
